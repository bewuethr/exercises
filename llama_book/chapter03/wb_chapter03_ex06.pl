#!C:\strawberry\perl\bin\perl.exe

# Use the range operator to create a list of only lowercase, only uppercase and
# a list of both

print 'a'..'z', "\n";
print 'A'..'Z', "\n";
print 'A'..'Z', 'a'..'z', "\n"; # not possible with just one range operator
